// this will serve as the table user setup for people table in course

export default function Roster() {
    return (
        <h2>Roster</h2>
        // will call peopletable and give proper insert , should be like users.tsx in account
    )
}